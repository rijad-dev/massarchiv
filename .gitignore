# Secrets — NIEMALS committen (API-Keys werden in der App-UI eingegeben,
# eine .env wird vom Code nicht gelesen)
.env
.env.*

# Private Nutzerdaten (Koerperdaten-Profil, Uploads) — inkl. SQLite-WAL/SHM
wardrobe.db*
uploads/
uploads-trash/

# Dependencies & Builds
node_modules/
dist/

# Sonstiges
*.log
.DS_Store
Thumbs.db
.claude/settings.local.json
