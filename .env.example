# Hinweis: Der OpenAI-API-Key wird in den App-Einstellungen (UI) eingegeben,
# nicht aus dieser Datei gelesen. Die lokale .env dient nur als private Notiz
# und ist gitignored — NIEMALS committen.
API: sk-...
